# Backlog

Ideas not (yet) built.

- **Keep `.git` mode.** Everything today is tarball-based, like giget itself — no `.git`, no
  history. A mode that keeps `.git` (via `git clone --filter=blob:none --sparse`) is a separate,
  still-unbuilt path.
